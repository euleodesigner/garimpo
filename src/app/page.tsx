export default function Home() {
  return (
    <main className="min-h-screen bg-bg text-ink flex items-center justify-center">
      <div className="rounded-lg border border-line bg-card px-8 py-6 text-center">
        <h1 className="text-2xl font-semibold text-ink">Lista Garimpo</h1>
        <p className="mt-2 text-sub">Fase 0 — setup em andamento</p>
        <span className="mt-4 inline-block rounded bg-clay px-3 py-1 text-sm text-card">
          ação primária
        </span>
      </div>
    </main>
  );
}
